# HAP Skills 用户安装使用指南

> 📖 给下载你的 Skills 的用户看的完整指南

---

## 🎯 这是什么？

这是两个专为 **Claude Code** 设计的技能包，可以让 AI 助手更好地帮你开发明道云 HAP 应用。

### 包含的 Skills

1. **hap-view-plugin** - HAP 视图插件开发助手
2. **hap-v3-api** - HAP V3 API 使用助手

---

## 💻 系统要求

- ✅ **Claude Code**（必需）- AI 编程助手
- ✅ **macOS / Linux / Windows**
- ✅ **Node.js 18+**（如果使用 hap-view-plugin）
- ✅ **Python 3.8+**（如果使用脚本工具）

---

## 📥 安装方法

### 方法一：下载 ZIP 安装（推荐，最简单）

#### 1. 下载 Skills

从 GitHub Release 页面下载：
https://github.com/garfield-bb/HAP-Skills-Public/releases

下载以下文件：
- `hap-view-plugin.zip`
- `hap-v3-api.zip`

#### 2. 安装到 Claude Code

**macOS / Linux:**
```bash
# 创建 skills 目录（如果不存在）
mkdir -p ~/.claude/skills/

# 进入 skills 目录
cd ~/.claude/skills/

# 解压下载的 ZIP 文件
unzip ~/Downloads/hap-view-plugin.zip
unzip ~/Downloads/hap-v3-api.zip

# 验证安装
ls -la ~/.claude/skills/
```

**Windows (PowerShell):**
```powershell
# 创建 skills 目录
New-Item -ItemType Directory -Force -Path $env:USERPROFILE\.claude\skills

# 进入 skills 目录
cd $env:USERPROFILE\.claude\skills

# 解压 ZIP（使用资源管理器或命令行）
Expand-Archive -Path "$env:USERPROFILE\Downloads\hap-view-plugin.zip" -DestinationPath .
Expand-Archive -Path "$env:USERPROFILE\Downloads\hap-v3-api.zip" -DestinationPath .
```

#### 3. 验证安装成功

安装后，你的目录结构应该是：
```
~/.claude/skills/
├── hap-view-plugin/
│   ├── SKILL.md
│   ├── scripts/
│   └── references/
└── hap-v3-api/
    └── SKILL.md
```

---

### 方法二：Git Clone（适合开发者）

```bash
# 进入 Claude skills 目录
cd ~/.claude/skills/

# 克隆仓库
git clone https://github.com/garfield-bb/HAP-Skills-Public.git temp

# 复制 skills
cp -r temp/hap-view-plugin ./
cp -r temp/hap-v3-api ./

# 删除临时目录
rm -rf temp

# 验证安装
ls -la
```

---

## 🚀 如何使用

安装完成后，Skills 会自动集成到 Claude Code 中。

### 自动触发（推荐）

Skills 会根据你的对话内容自动激活，无需手动操作。

#### 示例 1: 开发 HAP 视图插件

```
你：我想开发一个 HAP 的甘特图插件

Claude：我看到你需要开发 HAP 视图插件。让我使用 hap-view-plugin
       skill 来指导你...

       首先，我们需要初始化项目...
```

**触发关键词：**
- "开发 HAP 插件"
- "HAP 视图插件"
- "自定义看板"
- "甘特图"、"地图视图"
- "@mdfe/view"

---

#### 示例 2: 使用 HAP API

```
你：如何通过 API 查询 HAP 中的产品数据？

Claude：我将使用 hap-v3-api skill 来指导你。

       首先，你需要配置 API 鉴权...
```

**触发关键词：**
- "HAP API"
- "API 调用"
- "查询数据"
- "筛选器"
- "HAP-Appkey"、"HAP-Sign"

---

### 手动调用（可选）

你也可以在 Claude Code 中手动调用 skill：

```bash
# 调用 HAP 视图插件开发 skill
/hap-view-plugin

# 调用 HAP V3 API 使用 skill
/hap-v3-api
```

---

## 💡 使用场景

### Skill 1: hap-view-plugin

**适用场景：**
- ✅ 开发 HAP 自定义视图插件
- ✅ 创建自定义看板、列表、甘特图
- ✅ 使用 @mdfe/view SDK
- ✅ 调试和发布插件

**不适用：**
- ❌ 通过 API 查询数据（用 hap-v3-api）
- ❌ 搭建独立网站（用独立的 API 集成）

**示例对话：**
```
你：帮我创建一个产品展示的自定义看板
你：如何在插件中获取当前视图的数据？
你：插件开发完成后怎么上传到 HAP？
```

---

### Skill 2: hap-v3-api

**适用场景：**
- ✅ 在独立项目中使用 HAP API
- ✅ 学习 API 调用方法
- ✅ 理解筛选器语法
- ✅ 处理各种字段类型

**不适用：**
- ❌ 开发 HAP 内的视图插件（用 hap-view-plugin）

**示例对话：**
```
你：如何在 React 项目中调用 HAP API？
你：筛选器怎么写？我要查询价格在 1000-5000 的产品
你：附件字段在 API 中是什么格式？
```

---

## 🔧 验证 Skills 是否生效

### 方法 1: 查看已安装的 Skills

在 Claude Code 中，skills 会自动加载。你可以尝试提问来测试。

### 方法 2: 手动调用测试

```bash
# 在 Claude Code 中输入
/hap-view-plugin
```

如果 skill 正常工作，Claude 会回应并提供指导。

### 方法 3: 检查文件

```bash
# 确认文件存在
ls ~/.claude/skills/hap-view-plugin/SKILL.md
ls ~/.claude/skills/hap-v3-api/SKILL.md
```

---

## ❓ 常见问题

### Q1: 安装后 Skills 没有自动触发？

**A:** 可能是关键词不够明确，尝试：
- 使用更具体的关键词（如 "开发 HAP 视图插件" 而不是 "开发插件"）
- 手动调用：`/hap-view-plugin` 或 `/hap-v3-api`
- 重启 Claude Code

---

### Q2: 找不到 `~/.claude/skills/` 目录？

**A:** 手动创建：
```bash
mkdir -p ~/.claude/skills/
```

---

### Q3: 解压后提示权限问题？

**A:** 给脚本添加执行权限：
```bash
cd ~/.claude/skills/hap-view-plugin/scripts/
chmod +x *.sh
```

---

### Q4: 我该选择哪个 Skill？

**A:** 参考下面的决策树：

```
你的需求是什么？
    ↓
┌───────────────────────────────┐
│ 在 HAP 内开发视图插件？         │
└───────────────────────────────┘
    ↓ 是              ↓ 否
使用 hap-view-plugin   ↓
                ┌──────────────────────┐
                │ 通过 API 操作 HAP？   │
                └──────────────────────┘
                    ↓ 是
                使用 hap-v3-api
```

---

### Q5: Skills 如何更新？

**A:** 重新下载最新版本的 ZIP，覆盖安装：
```bash
cd ~/.claude/skills/
rm -rf hap-view-plugin hap-v3-api
unzip ~/Downloads/hap-view-plugin.zip
unzip ~/Downloads/hap-v3-api.zip
```

---

### Q6: 可以同时使用两个 Skills 吗？

**A:** 可以！它们各自独立工作：
- 开发插件时用 hap-view-plugin
- 需要 API 知识时用 hap-v3-api

---

## 📚 进一步学习

### 官方文档
- [HAP 官方文档](https://help.mingdao.com)
- [HAP API 文档](https://api.mingdao.com/docs)
- [Claude Code 文档](https://github.com/anthropics/claude-code)

### 示例项目
Skills 中包含了完整的示例和脚本，安装后可以查看：
- `~/.claude/skills/hap-view-plugin/scripts/` - 实用脚本
- `~/.claude/skills/hap-view-plugin/references/` - 参考文档

---

## 🆘 需要帮助？

### 遇到问题？
1. 查看上面的常见问题
2. 在 GitHub 提 Issue：https://github.com/garfield-bb/HAP-Skills-Public/issues
3. 参与讨论：https://github.com/garfield-bb/HAP-Skills-Public/discussions

### 想要贡献？
欢迎提交 Pull Request 改进这些 Skills！

---

## ⭐ 觉得有用？

如果这些 Skills 帮到了你，请在 GitHub 给个 Star：
https://github.com/garfield-bb/HAP-Skills-Public

---

## 📄 许可证

MIT License - 可自由使用和修改

---

**🎉 安装完成！现在开始使用 Claude Code 开发 HAP 应用吧！**

---

## 📋 快速参考卡

```
┌─────────────────────────────────────────────────┐
│  HAP Skills 快速参考                            │
├─────────────────────────────────────────────────┤
│                                                 │
│  安装位置：~/.claude/skills/                    │
│                                                 │
│  手动调用：                                     │
│    /hap-view-plugin                            │
│    /hap-v3-api                                 │
│                                                 │
│  自动触发关键词：                               │
│    • "HAP 视图插件"                            │
│    • "HAP API"                                │
│    • "自定义看板"                              │
│    • "筛选器"                                  │
│                                                 │
│  更新方法：重新下载 ZIP 覆盖安装                │
│                                                 │
│  获取帮助：                                     │
│    https://github.com/garfield-bb/             │
│    HAP-Skills-Public/issues                    │
│                                                 │
└─────────────────────────────────────────────────┘
```

**打印或保存此卡片，方便随时查阅！**
